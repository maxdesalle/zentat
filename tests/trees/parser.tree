ParserTest
├── parseNumber
│   ├── given a bare number
│   │   ├── it parses an integer
│   │   ├── it parses a decimal
│   │   └── it parses zero
│   ├── given grouping separators
│   │   ├── given US grouping
│   │   │   ├── it reads a comma as thousands
│   │   │   └── it reads a dot as the decimal point
│   │   ├── given EU grouping
│   │   │   ├── it reads a dot as thousands
│   │   │   ├── it reads a comma as the decimal point
│   │   │   └── it keeps three decimals after the comma
│   │   ├── given Indian lakh grouping
│   │   │   └── it reads the two-digit groups
│   │   └── given Swiss apostrophe grouping
│   │       ├── it reads a straight apostrophe
│   │       └── it reads a typographic apostrophe
│   ├── given exactly three digits after a single dot
│   │   ├── given the caller expects US decimals
│   │   │   └── it reads them as a decimal
│   │   └── given the caller does not
│   │       └── it reads them as thousands
│   ├── given exactly three digits after a single comma
│   │   └── given the caller expects US decimals
│   │       └── it still reads them as thousands
│   ├── given more than three digits after a single dot
│   │   └── it reads them all as decimals
│   ├── given a magnitude on top of grouped digits
│   │   └── it applies the magnitude to the grouped number
│   ├── given both separators appear more than once
│   │   ├── it reads dots as grouping when the comma is the decimal
│   │   └── it reads commas as grouping when the dot is the decimal
│   ├── given a magnitude suffix
│   │   ├── it reads it case-insensitively
│   │   ├── it applies the right magnitude for each letter
│   │   └── it takes the suffix off before reading the separators
│   ├── given a spelled-out multiplier
│   │   ├── it matches the whole word rather than a prefix
│   │   ├── it multiplies rather than divides
│   │   ├── it handles compound multipliers
│   │   ├── it handles non-English words
│   │   └── it reads the word whatever its case
│   └── given input that is not a number
│       ├── it returns null for letters
│       ├── it returns null for an empty string
│       └── given separators with no digits between them
│           ├── it returns null for commas alone
│           ├── it returns null for dots alone
│           ├── it returns null for a mix of both
│           └── it returns null for a separator followed by letters
├── overlaps
│   ├── given the spans are disjoint
│   │   └── it reports no overlap
│   ├── given the candidate starts inside the other
│   │   └── it reports an overlap
│   ├── given the candidate ends inside the other
│   │   └── it reports an overlap
│   ├── given the candidate contains the other
│   │   └── it reports an overlap
│   ├── given the spans merely touch
│   │   └── it reports no overlap
│   └── at the boundaries
│       ├── it treats a span starting exactly where the other ends as disjoint
│       ├── it treats a span ending exactly where the other starts as disjoint
│       └── it treats identical spans as overlapping
├── isBetterMatch
│   ├── given the candidate starts earlier
│   │   └── it wins
│   ├── given the candidate starts later
│   │   └── it loses
│   ├── given both start at the same offset
│   │   ├── given the candidate is longer
│   │   │   └── it wins
│   │   └── given the candidate is no longer
│   │       └── it loses
│   └── given the candidate starts at the same offset as the other
│       └── it compares lengths rather than positions
├── extractPriceFromMatch
│   ├── given the match captured no digits
│   │   └── it reads nothing
│   ├── given the captured digits are not a number
│   │   └── it reads nothing
│   ├── given the captured amount is negative
│   │   └── it reads nothing
│   └── given the captured amount is zero
│       └── it reads the price
└── parsePrice
    ├── given a symbol before the amount
    │   ├── it reads a dollar sign
    │   ├── it reads a euro sign
    │   └── it reads a pound sign
    ├── given the page declares a language
    │   ├── given the language uses a comma decimal
    │   │   └── it reads the number that way
    │   ├── given the language uses a dot decimal
    │   │   └── it reads three digits as cents
    │   ├── given the page declares no language
    │   │   └── it assumes a dot decimal
    │   ├── given the same language twice
    │       ├── it reads it the same way both times
    │       └── it asks the browser about that language only once
    │   └── given the language tag is not one the browser knows
    │       └── it falls back to a dot decimal rather than failing
    ├── given a whole-euro price closed with ",-"
    │   ├── it reads the mark as part of the price
    │   ├── it leaves a minus sign on the next number alone
    │   └── it does not claim it for another currency
    ├── given a price qualified by a tax label
    │   ├── it reads the number and leaves the label on the page
    │   └── it still swallows a currency NAME, which the ZEC replaces
    ├── given a code after the amount
    │   └── it reads the code
    ├── given a code before the amount
    │   └── it reads the code without the space in front of it
    ├── given several prices in one string
    │   └── it returns each of them
    ├── given a magnitude in the price
    │   ├── it reads a suffix letter
    │   └── it reads a spelled-out word
    ├── given a negative amount
    │   ├── it skips the negative price
    │   ├── it still reads both ends of a range
    │   ├── it treats every dash the web uses as a separator
    │   ├── it looks only at the character beside the dash
    │   └── given the sign is attached to another number
    │       └── it is not treated as a minus
    ├── given a currency that prices in cents
    │   └── it reads three digits after the dot as cents
    ├── given a price of zero
    │   └── it reads it as zero
    ├── given an ambiguous symbol
    │   ├── given the page states its own currency
    │   │   ├── it beats the guess from the hostname
    │   │   └── given the page names a currency the symbol cannot mean
    │   │       └── it ignores the page and keeps the guess
    │   ├── given the hostname names a country
    │   │   └── it resolves to that country's currency
    │   ├── given that currency is not enabled
    │   │   └── it falls back to an enabled candidate
    │   ├── given the page language disambiguates
    │   │   └── it resolves from the language
    │   └── given nothing disambiguates
    │       └── it resolves to the default reading
    ├── given a dollar sign claimed by a prefix
    │   ├── given the prefix names a supported currency
    │   │   └── it reads that currency
    │   └── given the prefix names an unsupported one
    │       └── it reads nothing rather than guessing US dollars
    ├── given a hostname-restricted pattern
    │   ├── it applies on that site
    │   ├── it applies on the bare domain as well as a subdomain
    │   ├── it does not apply to a host that merely ends in the same letters
    │   ├── it does not apply elsewhere
    │   ├── it reads a site's split euro-and-cent markup
    │   ├── it reads that markup when the price has no cents
    │   └── it keeps a single-digit cent in the cents place
    ├── given the same price appears twice in one string
    │   ├── it returns both of them
    │   └── it keeps them at their own positions
    ├── given two readings overlap in position
    │   ├── it keeps the longer match
    │   ├── it keeps the one that starts earlier
    │   ├── it discards the shorter one
    │   └── it keeps the reading whose symbol is actually in the text
    ├── given several prices out of source order
    │   └── it returns them left to right
    ├── given a currency is not enabled
    │   ├── it returns nothing for that currency
    │   └── given the symbol could mean an enabled one instead
    │       └── it uses the enabled reading
    └── given text with no price in it
        └── it returns nothing
